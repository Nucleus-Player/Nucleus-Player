// DEV: Currently only here for dev purporses, this file will be removed before
//      release
const webView = document.querySelector('webview');
webView.addEventListener('did-start-loading', () => {
  // webView.openDevTools();
});
